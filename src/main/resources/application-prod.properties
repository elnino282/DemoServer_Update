# Production configuration for Render deployment
server.port=${PORT:5432}

# Database configuration for production (PostgreSQL)
spring.datasource.url=${postgresql://agentmanagement_user:FuBPakB8bNwbmQlDfHMzlWuPjB3MZrqY@dpg-d165kom3jp1c73fb6uu0-a/agentmanagement}
spring.datasource.username=${agentmanagement_user}
spring.datasource.password=${FuBPakB8bNwbmQlDfHMzlWuPjB3MZrqY}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Logging
logging.level.root=INFO
logging.level.org.springframework.web=INFO

# Disable H2 console in production
spring.h2.console.enabled=false

# CORS configuration (if needed)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=* 